# ============ DATABASE CONFIG ============
# PostgreSQL Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mydb
DB_HOST=postgres
DB_PORT=5432

# ============ SERVER CONFIG ============
PORT=5000

# ============ JWT CONFIG ============
# Change this to a strong secret key in production!
JWT_SECRET=aether_protocol_secret_2026_change_this_in_production

# ============ ENVIRONMENT ============
NODE_ENV=development